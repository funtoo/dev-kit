<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>senyai@gmail.com</email>
		<name>Arseniy Terekhin</name>
	</maintainer>
	<use>
		<flag name="openssl">Look for OpenSSL in the given path, automatically, in the source tree, or none</flag>
		<flag name="zlib">Look for zlib in the given path, automatically, or in the source tree</flag>
		<flag name="sqlite">Look for sqlite in the given path, automatically, or in the source tree.</flag>
		<flag name="exec-rel-paths">Enable relative paths for external diff/gdiff</flag>
		<flag name="th1-docs">Enable TH1 for embedded documentation pages</flag>
		<flag name="th1-hooks">Enable TH1 hooks for commands and web pages</flag>
		<flag name="tcl">Enable Tcl integration, with Tcl in the specified path</flag>
		<flag name="tcl-stubs">Enable Tcl integration via stubs library mechanism</flag>
		<flag name="tcl-private-stubs">Enable Tcl integration via private stubs mechanism</flag>
		<flag name="mman">Enable use of POSIX memory APIs from "sys/mman.h"</flag>
		<flag name="see">Enable the SQLite Encryption Extension (SEE)</flag>
		<flag name="print-minimum-sqlite-version">print the minimum SQLite version number required, and exit</flag>
		<flag name="internal-sqlite">Don't use the internal SQLite, use the system one</flag>
		<flag name="static">Link a static executable</flag>
		<flag name="fusefs">Disable the Fuse Filesystem</flag>
		<flag name="debug">Build with fossil debugging enabled</flag>
		<flag name="no-opt">Build without optimization</flag>
		<flag name="json">Build with fossil JSON API enabled</flag>
		<flag name="emsdk">Directory containing the Emscripten SDK</flag>
	</use>
</pkgmetadata>