input-pad:
  generator: github-1
  packages:
    - input-pad:
        github:
          user: fujiwarat
          repo: input-pad
          query: releases
          tarball: "input-pad-{version}.tar.gz"

